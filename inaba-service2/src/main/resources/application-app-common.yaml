server:
  port: ${SERVER_PORT}

grpc:
  server:
    port: ${GRPC_PORT}

spring:
  application:
    name: ${APPLICATION_NAME}

axon:
  serializer:
    general: jackson
    events: jackson
    messages: jackson