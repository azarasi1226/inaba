server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: inaba-identity-service

axon:
  serializer:
    general: jackson
    events: jackson
    messages: jackson

aws:
  cognito:
    client-id: ${AWS_COGNITO_CLIENT-ID}